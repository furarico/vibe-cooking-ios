version: "3"

vars:
  FASTLANE: bundle exec fastlane

tasks:
  default:
    cmds:
      - task --list
    silent: true
  install:
    desc: "Install"
    cmds:
      - "bundle install"
  test:
    desc: "Test"
    cmds:
      - "{{.FASTLANE}} test"
